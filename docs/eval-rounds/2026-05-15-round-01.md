# scripts-eval Round 01 — 2026-05-15

Run id: `2026-05-15-round-01`
Corpus: `experiments/scripts_eval/corpus.yaml` (corpus_version: 1)
Trials per cell: 3
Arms: A (no `repo-map` skill), C (may use `repo-map`)

This file is the round's **evidence accumulator**. The two
`<!-- runstate:... -->` / `<!-- evidence:... -->` regions below are
rewritten by `experiments/scripts_eval/summarize.py` at the end of every
session — operator never edits them by hand.

Raw per-cell JSONs stay gitignored under
`experiments/scripts_eval/results/2026-05-15-round-01/`. This file is
the committed evidence.

## Procedure

The operator procedure is locked in the **`eval` skill** —
`.claude/skills/eval/SKILL.md`. Read or invoke that skill from a fresh
session; this file does not duplicate it.

## Per-session preflight (your shell, *before* `claude`)

Source `experiments/scripts_eval/switch-arm.sh` — it exports
`SEER_EVAL_RUN_ID` + `SEER_EVAL_ARM` in your shell **and** moves
`.claude/skills/repo-map` aside (arm A) or restores it (arm C),
idempotently:

```bash
# arm-A session:
source experiments/scripts_eval/switch-arm.sh A 2026-05-15-round-01

# arm-C session (same run id, flipped arm):
source experiments/scripts_eval/switch-arm.sh C 2026-05-15-round-01
```

Then launch `claude` from the seer-cli repo root. The script's
disk-state move is shared across all sessions in the repo, so don't
flip arms while a sibling session for the other arm is mid-run.

## Paste templates

Once inside the fresh `claude` session, paste the matching template.
Each template is a short trigger — the `eval` skill loads the locked
procedure automatically.

### Arm-A session

```text
Run scripts-eval round 01: arm A of {TARGET}/{QUESTION_ID} × 3 trials.
Run id: 2026-05-15-round-01.

Use the `eval` skill. After all 3 trials are captured, summarize against
docs/eval-rounds/2026-05-15-round-01.md and commit.
```

### Arm-C session

```text
Run scripts-eval round 01: arm C of {TARGET}/{QUESTION_ID} × 3 trials,
then judges and commit.
Run id: 2026-05-15-round-01.

Use the `eval` skill. Confirm the matching arm-A cells exist on disk
before starting; abort if not.
```

Replace `{TARGET}` with one of `agtag` / `culture` / `daria` /
`claude-code-guide` / `citation-cli`, and `{QUESTION_ID}` with one of
`q-profile-overview` / `q-connections-1hop` / `q-narrative` /
`q-transfer-cli` / `q-transfer-quality` (or `q-graph-workspace` for the
workspace-scope question — that one substitutes `{workspace_root}` and
omits `--target` on `trial start`). The run-state table below is the
source of truth for what's still pending.

## Run state

<!-- runstate:start -->

| target | question | arm-A | arm-C | judged | winners (A/C/tie) |
|---|---|---|---|---|---|
| agtag | q-profile-overview | 3/3 | 3/3 | 3/3 | 1/2/0 |

<!-- runstate:end -->

## Evidence per set

<!-- evidence:start -->

### agtag / q-profile-overview

Winners: **A=1, C=2, tie=0** (of 3 judged).

| trial | winner | margin | A duration | C duration | A tools | C tools | judge reasoning |
|---|---|---|---|---|---|---|---|
| 1 | C | slight | 59.7s | 60.1s | Bash:17, Read:13 | Bash:9, Read:10 | Both answers are factually similar and well-structured, but X provides more granular component detail (e.g., issue.py as dispatcher, distinct universal vs. issue commands, docs/ breakdown) and explicit roadmap context, while Y is slightly more concise but adds the unverified '576 lines' figure and merges details more loosely. |
| 2 | A | slight | 45.9s | 37.4s | Bash:10, Read:23 | Bash:7, Read:17 | Y gives a more accurate file tree, exact test count, coverage gate, command signatures, and dependencies, while X invents some paths (e.g., agtag/issue/ vs commands layout) and an unverified 846 LOC/85% figure. |
| 3 | C | slight | 76.6s | 40.1s | Bash:12, Read:18 | Bash:10, Read:12 | Y covers more top-level structure (docs/, vendored skills/, config files) and adds PyPI publish workflow detail, making it more complete and actionable, though both answers are similar in factual accuracy. |

<!-- evidence:end -->
